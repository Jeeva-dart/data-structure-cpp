#ifndef RECTANGLE_H
#define RECTANGLE_H
#include <iostream>
using namespace std;
class Rectangle {
    private:
        short width;
        short height;

    public:
        static short count;
        Rectangle(short width);
        Rectangle(const Rectangle& source);
        ~Rectangle();
        short getWidth() const;
        void setWidth(short width);
        void draw();
        int getArea();
        bool operator==(const Rectangle& rect) const;
};


#endif